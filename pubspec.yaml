name: ondrop
description: Send files without compression. No quality loss.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  firebase_storage: ^11.2.7
  file_picker: ^6.1.1
flutter:
  uses-material-design: true
